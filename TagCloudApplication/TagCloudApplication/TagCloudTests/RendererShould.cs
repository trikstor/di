using System;
using System.Drawing;
using FluentAssertions;
using NUnit.Framework;
using TagCloud;
using TagCoudRenderer;

namespace TagsCloudVisualization
{
    [TestFixture]
    public class RendererShould
    {
        /*
        private Renderer TagCloudRenderer;
        
        [SetUp]
        public void SetUp()
        {
            TagCloudRenderer = new Renderer(new Point(250, 250), Brushes.Black, "Arial", new Size(500, 500));
        }
        
        [Test]
        public void ThrowException_InvalidOutputPath()
        {
            Action result = () => { TagCloudRenderer.Draw("/invalidPath/", null); };
            result.ShouldThrow<ArgumentException>().WithMessage("Неверный путь для изображения.");
        }
        */
    }
}